---

# -r ... Combine the realm with the login (with an '@' sign in between)
# -c ... Enable caching of authentication credentials
# -m ... Use path as the pathname to the named socket to listen on for connection requests.
saslauthd_options_default: "-r -c -m {{ saslauthd_socket }}"
